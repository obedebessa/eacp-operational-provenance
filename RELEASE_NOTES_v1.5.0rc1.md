# Software 1.5.0rc1: bounded operability and review delivery

Status: release candidate, not 1.5.0 final. Profile and paper remain 1.3. This
release adds a runnable operational interface, not a new scientific protocol.

## Implemented and measured at e2807efc

- Installed CLI for ingestion, scoped queries, export and checkpoint-bound verification.
- Atomic cursor pages, diagnostics, tenant-bound backup and new-destination restore.
- 276 author-run tests across five suites; 52 optimized-mode repeats are a subset.
- A 17-command synthetic journey, three specific mutation sentinels, and five
  predeclared bounded workload scenarios. No enterprise capacity or long soak claim.

## Supplied external execution

The returned records identify the same original candidate and module hashes.
Initial execution recorded 275 passed and one skip among 276 tests; the historical
cryptographic test subsequently passed with GitHub CLI available. The follow-up
hardening run recorded 112 passed without skips. Preserve both phases instead of
rewriting the first run as if no skip occurred. See the [record](docs/v1.5/EXTERNAL_EXECUTION.md).

The public copy uses a neutral executor identifier; identifying records are held
separately. This is supplied reexecution evidence, not authenticated identity,
independent scientific peer review or a guarantee of security.

## Delivery corrections, not new measurements

- One lineage card replaces competing version banners.
- Private filesystem prefixes and reviewer identities are hidden in explicitly
  labeled copies. PDF contact-only privacy derivatives preserve scientific text.
- A redaction ledger distinguishes original and distributed byte hashes.
- Public packaging excludes Git history and stale internal manifests, uses the
  latest supplied summary, includes the historical-crypto addendum, and generates
  fresh checksums. Original submissions remain separately preserved.
- The privacy scanner checks paths, reviewed identity literals, nested containers
  and selected encoded content without modifying signed statements.

The execution commit and the delivery commit may differ. Compare module hashes;
do not claim the external executor tested later packaging/privacy code.

## Explicit non-claims

No new paper or Profile version; no new live 1.5 signature; no new Kubernetes
experiment; no authorized field pilot; no SLSA L3 or production certification;
no 208 completed scenarios; no exhaustive mutation coverage. Internally consistent
false source assertions remain outside the resolver's truth guarantees.

The original 1.3/1.4 tags and signed TARs retain their original identities.
Contact/path removal changes distribution bytes and does not rewrite old public
history or an existing DOI. An old signature never authenticates the later ZIP.
