[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "eacp-operational-provenance"
version = "1.5.0rc1"
description = "EACP bounded operability candidate with preserved Profile 1.3 semantics"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "Apache-2.0" }
authors = [
  { name = "Obede Bessa Rocha da Silva" }
]
keywords = [
  "operational provenance",
  "cloud-native",
  "Kubernetes",
  "OpenTelemetry",
  "reproducibility"
]
dependencies = ["cryptography==50.0.1"]

[project.optional-dependencies]
figures = ["Pillow>=10,<13"]
hardening = ["cryptography==50.0.1"]

[project.scripts]
eacp-hardening = "eacp_hardening.cli:main"

[project.urls]
Repository = "https://github.com/obedebessa/eacp-operational-provenance"

[tool.setuptools]
packages = ["eacp_hardening", "eacp_profile"]
package-dir = { eacp_profile = "spec/tools" }

[tool.unittest]
start-directory = "tests"
